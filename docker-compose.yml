version: "3"

services:
  backend:
    image: backend:backend
    ports:
      - "8000:8080"

  frontend:
    image: frontend:frontend
    ports:
      - "80:3000"
#     environment:
#       API_URL: backend:8000
